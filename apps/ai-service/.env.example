# AI microservice environment (copy to apps/ai-service/.env)

# Free key from https://aistudio.google.com/apikey (no credit card).
GEMINI_API_KEY="your-ai-studio-key"

# Must match INTERNAL_API_KEY in apps/web/.env
INTERNAL_API_KEY="generate-a-long-random-string"

PORT=4000
